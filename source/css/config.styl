// =============================================================================
// Config variables.
// =============================================================================
// Define here your global variables.
//
// Your variables should start with `$`, like the examples below.
// =============================================================================

// Breakpoints
// -----------

$bp = {
  xlg: 1600px,
  xg: 1280px,
  lg: 1024px,
  md: 800px,
  sm: 600px,
  xs: 400px
}

// Colors
// ------

$color = {}

// Main colors
$color.primary = #00a2dc
$color.secondary = #1a293f
$color.success = #2ecc71
$color.danger = #e74c3c
$color.warning = #f1c40f
$color.title = #1a293f
$color.text = #5a5a5a

// Social colors
$color.facebook = #3b5998
$color.twitter = #55acee
$color.google = #dd4b39
$color.youtube = #bb0000
$color.linkedin = #007bb5

// Gray shades
$color.gray-light = #f2f2f2
$color.gray-medium = #bbbbbb
$color.gray-dark = #545454
$color.gray-x-dark = #303030
$color.gray-x-x-dark = #333

// Basic colors
$color.white = #fff
$color.black = #101010

// Container
// ---------

$container = {
  width: 1320px
  padding: 20px
}

// Spacing
// -------

$spacing = {
  none: 0,
  x-x-small: 3px,
  x-small: 5px,
  small: 10px
  normal: 15px,
  medium: 20px,
  large: 25px,
  x-large: 30px,
  x-x-large: 35px,
  huge: 40px,
  x-huge: 45px,
  x-x-huge: 50px
}

// Font Family
// -----------

$font-family = {
  primary: 'Sarabun'\, sans-serif
}

// Font size
// ---------

$font-size = {
  x-small: 12px,
  small: 14px,
  normal: 16px,
  medium: 18px,
  large: 24px,
  x-large: 30px,
  x-x-large: 36px,
  huge: 40px
}

// Icon selector
// -------------

$icon-selector = '[class^="icon-"]'


$gradient-main = linear-gradient(250deg, #48aef4, #0778c6)
$gradient-dark = linear-gradient(to bottom, #666982, #1a293f)